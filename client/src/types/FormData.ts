export type FormDataRegister = {
    fullName: string;
    email: string;
    password: string;
    confirmPassword: string;
    telephone: string;
};
export type FormDataLogin = {
    email: string;
    password: string;

};
